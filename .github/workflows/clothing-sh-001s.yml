name: Split clothing shoes 001 Images

on:
  workflow_dispatch:

  schedule:
    - cron: '0 3 * * *'  # هر روز 06:00 صبح ترکیه (UTC 3:00)

  push:
    paths:
      - 'daily/clothing/shoes/sh-001.png'
      - 'scripts/clothing/sh-001s.mjs'
      - '.github/workflows/clothing-sh-001s.yml'

jobs:
  split-shoes-images:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install canvas dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev

      - name: Install npm packages
        run: npm install

      - name: Run shoes splitter script
        run: node scripts/clothing/sh-001s.mjs

      - name: Commit and push shoes sliced images
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add daily/clothing/shoes/sliced/
          git commit -m "Auto-split: updated sliced images from sh-001.png" || echo "No changes to commit"
          git pull --rebase
          git push
