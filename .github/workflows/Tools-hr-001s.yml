name: Split Tools hardware 001 Images

on:
  workflow_dispatch:

  schedule:
    - cron: '0 3 * * *'  # هر روز 06:00 صبح ترکیه (UTC 3:00)

  push:
    paths:
      - 'daily/Tools/hardware/hr-001.png'
      - 'scripts/Tools/hr-001s.mjs'
      - '.github/workflows/Tools-hr-001s.yml'

jobs:
  split-Tools-hr-001s-images:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install canvas dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev

      - name: Install npm packages
        run: npm install

      - name: Run hr-001s splitter script
        run: node scripts/Tools/hr-001s.mjs

      - name: Commit and push hr-001s sliced images
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add daily/Tools/hardware/sliced/ || echo "No sliced images to add"
          git commit -m "Auto-split: updated sliced images from hr-001.png" || echo "No changes to commit"
          git fetch origin main
          git pull --rebase
          git push
