name: Generate Daily Images

on:
workflow_dispatch:

jobs:
build:
runs-on: ubuntu-latest
defaults:
run:
working-directory: .

steps:
- name: Checkout repo
uses: actions/checkout@v3
with:
path: .

- name: Setup Node.js
uses: actions/setup-node@v3
with:
node-version: '20'
